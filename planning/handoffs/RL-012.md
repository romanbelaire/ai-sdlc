# RL-012 handoff

- Owner: agent-session-1 (cursor-20260919-1459); branch
  `feature/RL-012-onboarding`; worktree `.agent-state/worktrees/RL-012`;
  base `30f0a38`.
- Outcome: `hub.py help [keyword]` plus a load-bearing AGENTS.md stanza.
  `agent-infra` stayed with session-2 (RL-011); help lives in
  `help_topics.py` with a small `Hub.help` hook.
- Files: `tools/agent_hub/help_topics.py`, `tools/agent_hub/hub.py`,
  `tools/agent_hub/tests/test_hub.py`, `AGENTS.md`, `HANDBOOK.md`.
- Next: independent review of the head SHA; merge only with Roman's OK.
  Rebase if RL-011 lands hub.py first.

## Independent review of `1d82e75`

- Reviewer: agent-session-2 (`codex-20260919-1545`). Verdict: request changes.
- Finding 1: `help` describes `RL_AGENT_HUB_DB` but never emits the actual
  absolute database path, so the advertised command list does not satisfy the
  absolute shared-database acceptance criterion. Add a structured absolute path
  derived from the active `Hub.path` and assert it in the CLI/unit tests.
- Finding 2: AGENTS.md says both “do not start by reading HANDBOOK.md” and
  “start ... and read HANDBOOK.md.” Make startup order unambiguous and retain
  the load-bearing help stanza.
- Evidence: all 20 hub tests passed; `python tools/check_repo.py` passed; all
  eight topic payloads were nonempty and under 2500 characters; unknown topic
  exited nonzero. No unrelated scope findings.

## Review resolution

- Implementer: agent-session-1 (`codex-20260919-1550`). Both findings fixed:
  every help payload now contains the resolved absolute `db_path` and a
  copyable `db_command`, with unit coverage; AGENTS.md now points to the paths
  returned by help instead of unconditionally requiring HANDBOOK.md.
- Validation: 20/20 hub unit tests passed; `python tools/check_repo.py` passed;
  AGENTS.md is 3939 characters; `git diff --check` passed.
- Delta review: agent-session-2 (`codex-20260919-1552`) reviewed fixed head
  `f76955e` and approved it with no remaining findings. Reviewer reran all
  20 hub tests and `tools/check_repo.py`, verified the active absolute DB path
  and copyable command through the CLI, and confirmed the delta is scope-clean.
- Next: integrator may move RL-012 to review; merge only with Roman's OK.
