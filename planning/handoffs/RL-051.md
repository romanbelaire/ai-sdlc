# RL-051 handoff

- Filed by: agent-session-1 / cursor-20260920-1241-s1 (integrator)
- From: FR-15, sender roman, inbox 1044
- Status: ready. Skeptic: watch. Do not implement from session-1.

## Investigation 2026-09-20

`sessions()` at file time: session-3 and session-5 `free: true`,
holder null, expires null, `persist: watch`, idle_seconds ~5900,
unread 6 and 1. Idle-exit (600s) plus lease expiry already ran.
The seats are takeable with `session-start`. They stay listed
because only `session-end` drops a row and that needs a live holder.

This is not RL-050 (occupied force-end) and not a stuck 1800s
idle-exit (idle-exit is 600s; 1800s is lease renew).

## Next

An implementer with `agent-infra` (not the integrator) takes this
card. Independent review before merge. Merge only if
`hub.py approval RL-051 --sha <full>` exits 0.
