# RL-017 handoff

- Session: `agent-session-3`; holder `codex-20260919-merge-approval`.
- Branch: `feature/RL-017-dashboard-merge-queue` from main `1a9bd96`.
- Scope: review-gated dashboard merge queue. No product, contract, or
  automatic Git merge behavior.
- Claims: `RL-017`, `agent-infra`.

## Implementation

- Added durable exact-SHA review artifacts. `review-submit` requires a live
  reviewer session different from the author, records blocking and
  non-blocking comments, notifies the author/integrator, and supersedes older
  approvals for the ticket.
- Added a dedicated dashboard Merge tab. Only the newest clear review whose
  branch still resolves to the reviewed SHA appears. The old arbitrary branch
  chooser was removed.
- Approve creates a signed, exact-SHA approval linked to the review and only
  notifies the integrator. Send with comments records changes requested,
  revokes approval, and notifies the author/integrator.
- Added `chat-approval` for the live integrator to record Roman's explicit yes
  from its direct chat as `source=chat`; it still requires the newest clear
  review and exact SHA. Hub messages remain non-authoritative.
- Approval signature version 2 covers `source` and `review_id`; existing v1
  approval rows remain verifiable after migration.

## Validation

- `python -m unittest discover -s tools/agent_hub/tests -v` — 38 passed.
- `python tools/check_repo.py` — passed; 17-ticket DAG and Python syntax OK.
- `git diff --check` — passed (Git reports only the repository's expected
  LF-to-CRLF checkout warning for `dashboard.html`).
- MCP surface was not changed, so the locked MCP smoke test is not required.

## Review focus

- Confirm a stale/moved head cannot be approved from the dashboard.
- Confirm a newer review or Send with comments cannot leave an older approval
  active.
- Confirm `chat-approval` is restricted to the configured live integrator and
  cannot bypass the independent clear review.
- Inspect SQLite migration and v1/v2 HMAC compatibility.

## Remaining limitations

- Branch freshness is checked against local refs in the integrator checkout;
  remote-only branch movement is intentionally out of scope.
- `review-submit` is a CLI workflow; no new MCP write tools were added.

## First independent review

Agent-session-2 reviewed `b5ae52f` and requested two blocking fixes plus one
audit improvement:

- Unknown/operator reviewer IDs and a shared holder could bypass independence;
  the declared author was trusted. Fixed by requiring live configured author
  and reviewer sessions, deriving author from the live ticket claim, rejecting
  claim mismatches/self-review, and rejecting a holder shared by both sessions.
  The same strict holder check now protects `chat-approval`.
- Lease renewals caused the Merge view to redraw and erase typed comments.
  Fixed with per-review drafts captured/restored across polling redraws.
- Chat approvals attributed their notification to Roman without recording the
  integrator. V2 signatures now cover `recorded_by` and `recorded_holder`, and
  chat notifications originate from the recording integrator session.

The public unreviewed `Hub.approve()` path was removed. Regression probes cover
unknown/operator reviewers, author mismatch, reviewer-owned ticket, shared
holder, unstarted chat recorder, draft-preservation markup, provenance, and
idempotent approval.

Post-fix validation: 39/39 hub/dashboard tests, `check_repo`, dashboard
JavaScript syntax, and `git diff --check` pass.

Agent-session-2's focused re-review approved the identity/provenance fixes but
found that polling restored draft text without restoring focus, so keystrokes
after a redraw could be lost. The Merge renderer now remembers the focused
review ID and selection range, then refocuses the replacement textarea and
restores its caret/selection. Documentation also states that the author must
renew the ticket claim while review is pending; the watch loop renews only the
session lease.
