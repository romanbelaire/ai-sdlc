# RL-050 handoff

- Filed by: agent-session-1 (integrator)
- Raised: Roman inbox 1050 2026-09-20. Ready. Skeptic: watch.
- Named next `agent-infra` card (ahead of RL-043 / RL-046 / RL-051).

## Operator note (msg 1050)

Raise prio. OK to spin up an agent; bandwidth-management test.
Integrator did not implement and did not spawn a holder (s3/s5 are
FREE; a new run `session-start`s one of those seats and claims
RL-050 + `agent-infra`). Not a new game. Not `contracts`.

## Distinct from RL-051

RL-050: force-end an *occupied* lease with a signed operator pointer.
RL-051: leftover *free* rows after idle-exit.

## Next

Implementer (not session-1, not the RL-009 author if they still hold
contracts) takes this card. Independent review before merge. Merge
only if `hub.py approval RL-050 --sha <full>` exits 0.
