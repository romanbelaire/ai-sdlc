# RL-028 handoff

- Owner: agent-session-3 / cursor-20260920-0013-s3
- Branch/worktree: feature/RL-028-holder-persist at
  C:\path\to\repo\.agent-state\worktrees\RL-028
- Base: main 1ef3b5ecbf4b091f197565f5bdc4f7840b7b6145
- Head: 1ad227e920efcfba8faa26126df7a2a6f0f259b5
- Claims: RL-028, agent-infra. Did not take other tickets or reserved resources.

## Outcome

Named holder persist on the hub API with no new loop, daemon, sweeper, busy
flag, table, or schema.

- `persist_mode(body)` reads a locker line `persist: oneshot` or
  `persist: watch`. Absent persist line means oneshot.
- `locker-save` rejects any other persist value and more than one persist line.
- `sessions()` includes `persist` as `oneshot` or `watch`.
- `help sessions` and `help watch` name both modes and forbid a second poller
  or a session-start heartbeat.
- Product listeners stay docs-only adapters on existing watch sentinels.

## Files

- tools/agent_hub/hub.py
- tools/agent_hub/help_topics.py
- tools/agent_hub/server.py (MCP docstrings only)
- tools/agent_hub/tests/test_hub.py
- AGENTS.md, docs/development.md, HANDBOOK.md
- planning/features/RL-028-holder-persist.md (unchanged brief)
- planning/handoffs/RL-028.md

## Validation

- `python -m unittest discover -s tools/agent_hub/tests -v` — 49 passed
  (worktree, CUDA_VISIBLE_DEVICES empty), including persist sessions/reject
  and help naming tests.
- `python tools/check_repo.py` — Infrastructure OK.
- `git diff --check` — clean.
- No GPU. No MCP smoke (docstrings only; tool count unchanged).

## Integration

Merged to main as `8592421` (parents `43050c2` + `1ad227e`). R-16 by
session-5, A-11 by roman via dashboard. Rechecked after merge:
`python -m unittest discover -s tools/agent_hub/tests -v` 49 passed;
`python tools/check_repo.py` Infrastructure OK. R-16 notes remain
non-blocking.

## Next

Session-3 should release RL-028 and agent-infra.
